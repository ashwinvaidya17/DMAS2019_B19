using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BattlefieldSpawnAnimation : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Add spawn animation
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
